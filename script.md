桌宠要懂用户。

---

那它需要哪些数据？

---

这份 DRS，回答这个问题。

---

desktop-pet 的数据需求规格说明书。

---

8 个主题走一遍。

---

数据边界。五类循环。13 模块。12 开关。

---

画像 7 子对象 + 元字段。角色相似度。最后是数据契约。

---

每个都对应同一组动作。
能采、能整理、能查询、能删除、能更正。

---

先讲数据边界。文档分 4 类。

---

第一类，核心业务数据。

---

用户配置、首方游戏数据、首方对话、角色相似度结果。
默认可用。但用户优先级最高。

---

第二类，授权增强数据。默认全关。按 app、按场景启用。

---

包含：低敏 PC context、MCP app 信号、VLM 画面理解、系统音频、产品内模型分析。

---

第三类，语义结果保留边界。

---

原图丢。原始音频丢。raw OS context 丢。

---

只留派生语义。VLM 场景标签、active app 名、输入强度桶、app 摘要、画像项。

---

第四类，最硬的——禁止采集与回写。

---

Recall 式后台截图，不接。
键盘文本、输入字符、keylog，不存。
第三方聊天、邮件、文档、会议正文，不读。

---

原始截图、原始系统音频，不上传、不训练、不留帧日志。
VLM 看屏幕，只吐语义标签——比如 boss_fight、low_hp。

---

人声、转写、通话内容，不采、不转、不写。

---

这是 5 条死禁红线。

---

不是承诺，是字段表里逐项落地的边界。

---

讲完边界，讲数据循环。

---

桌宠和记忆系统之间，数据流动有 5 条路径。

---

第一条，用户配置。
昵称、关系偏好、打扰边界、授权、删除、清空、重新总结。
优先级最高。AI 不能动用户改过的字段。

---

第二条，记忆系统供给桌宠。
用户画像、长期记忆、聊天摘要、游戏进度、成就、高光、来源与证据。
每条都能反查来源。

---

第三条，桌宠侧实时生成。
对话、游戏建议、复盘草稿、当前上下文、VLM 标签。
默认不进长期记忆。

---

第四条，桌宠回写记忆。
首方对话、用户反馈、用户更正、AI 推断画像项、高光、角色相似度结果。
回写必须带来源、时间、证据。

---

第五条最重要——压根不进循环。
原始截图、原始音频、按键内容、第三方正文、raw OS context。
不采、不存、不回写。

---

接下来是 13 个数据模块。

---

chat 聊天数据、PC 进程行为、UI 行为操作、MCP app 信号。

---

游戏 idip 状态、游戏实时事件、VLM 画面理解。

---

current_context 当前上下文、profile_meta 元字段、profile 用户画像。

---

highlight_event 高光、user_preferences 用户偏好。

---

最后一个，game_character_similarity_assessment 角色相似度测定。

---

13 个模块，分 P0 和 P1。
P0 是首版体验最小数据闭环。P1 是授权增强能力。

---

每个模块在文档里都有完整字段表、字段语义、隐私边界、Schema 示例。

---

接下来是用户开关。一共 12 项。

---

P0 5 项，首启询问。
聊天入长期记忆、游戏事件长期画像、AI 推断画像项。
记忆管理、基础陪伴偏好。

---

P1 6 项，默认关。
行为数据画像、画面理解、界面文字读取。
MCP 接入、日记/高光生成、角色相似度。

---

扩展 1 项，默认关——系统音频音乐氛围感知。

---

你不主动勾，没一个会开。AI 也不许自己开。

---

你撤回授权，桌宠会再问一句。要不要顺便删历史？

---

撤回和删除，是两个动作。不混在一起。

---

接下来讲画像。7 个子对象。

---

profile_identity 基础昵称、pet_relationship 桌宠关系、game_profile 游戏偏好。

---

playstyle_profile 玩法风格、companion_profile 陪伴偏好。

---

progress_profile 进度成就、social_profile 社交偏好。

---

7 类都不写"现实人格"。不写"谨慎型"。不写"控制欲强"、"玻璃心"。

---

只写"游戏内行为偏好"。比如稳健发育、目标优先、团队支援。

---

每条画像字段，都拖着一份 profile_meta 元字段。

---

里面装四样核心字段。

---

confidence 置信度，4 档——high、medium、low、blocked。

---

source_category 证据来源域。

---

generation_method 生成方式——用户写的、系统记录的、还是 AI 推的。

---

evidence_ids 证据 ID 数组。加 first_seen_at、last_confirmed_at 时间戳。

---

还有 is_active + inactive_reason 失效记录。

---

为啥要存？你问桌宠："你凭啥说我爱稳健发育？" 它得答得上来。

---

最关键一条——你改过的画像，AI 不再覆盖。

---

"这不像"、"这不准"、"以后别这样记"。全是合法用户动作。

---

最后一个模块，角色相似度测定。

---

桌宠告诉你，你像游戏里的哪个角色。

---

但不是"你 82% 像 TA"那种装腔。是"在哪几个角色特点上像 TA"。

---

6 个固定维度。

---

玩法风格、游戏目标、成长路径、情绪反应、社交方式、内容偏好。

---

每个维度有 5 种判定状态。

---

matched、not_matched、uncertain、insufficient_evidence、not_authorized。

---

后三种不进总分。"未授权"不被当成"不符合"。

---

默认关。你每次主动点"测一次"，桌宠才跑。不做后台周期性重测。

---

测出来的结果，默认不影响日常陪伴。

---

除非你单独打开一个开关——"用于陪伴策略"。

---

讲完了。

---

DRS 回答的是一个问题——桌宠需要哪些数据。

---

答案是一份契约。
任何字段、任何分析、任何回写，都不能绕过用户授权、删除策略、用户更正。

---

可采集、可整理、可查询、可删除、可更正。
是数据需求，也是数据契约。
